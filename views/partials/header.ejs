<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>Paper Feedbacks</title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/https://fonts.googleapis.com/css?family=Lato">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,400">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto+Slab">
    <link rel="stylesheet" href="/fonts/font-awesome.min.css">
    <link rel="stylesheet" href="/css/Contact-Form-Clean.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <% if(!current_user){ %> 
    <nav class="navbar navbar-light navbar-expand-md">
        <div class="container"><a class="navbar-brand" href="/login">Paper</a><button data-toggle="collapse" class="navbar-toggler" data-target="#navcol-1"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
            <div class="collapse navbar-collapse justify-content-end" id="navcol-1">
                <ul class="navbar-nav">
                    
                    <li class="nav-item text-center"><a class="nav-link" href="/login">Login</a></li>
                    <li class="nav-item text-center"><a class="btn btn-primary" role="button" style="margin-top: 0px;color: var(--white);background: var(--cyan);border-color: var(--info);" href="/register">Register</a></li>
                </ul>
            </div>
        </div>
    </nav>
    <% } %> 
    <% if(current_user){ %>
        <nav class="navbar navbar-light navbar-expand-md">
            <div class="container"><a class="navbar-brand" href="/feedback/<%= current_user.username %>">Paper</a><button data-toggle="collapse" class="navbar-toggler" data-target="#navcol-1"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse justify-content-end" id="navcol-1">
                    <ul class="navbar-nav">
                        <% if(current_user.usertype != 'admin'){ %> 
                        <li class="nav-item text-center"><a class="nav-link" href="/feedback/<%= current_user.username %>/create">Create Feedback</a></li>
                        <% } %> 
                        <li class="nav-item text-center"><a class="nav-link" href="#"><%= current_user.username %> 
                            <% if(current_user.usertype == 'admin'){ %> <span>[Admin]</span> <% } %> 
                        </a></li>
                        <li class="nav-item text-center"><a class="nav-link" href="/logout">Logout</a></li>
                    </ul>
                </div>
            </div>
        </nav>
    <% } %> 